---
description: Surface read-only curation guidance: stale entries, orphans, sources without notes, aliasable topics.
argument-hint: [--json]
---
Run `node "${CLAUDE_PLUGIN_ROOT}/bin/research-vault.mjs" advise` and present the four signal sections as suggestions; never mutate the vault.
