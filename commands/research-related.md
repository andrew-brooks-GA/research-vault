---
description: Show entries related to an id (forward edges + backlinks).
argument-hint: [id]
---
Run `node "${CLAUDE_PLUGIN_ROOT}/bin/research-vault.mjs" related <id>` (add `--format mermaid` for a graph) and present the result.
