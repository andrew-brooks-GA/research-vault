# Research Vault

A type-organized cache of research artifacts. Agents start at `AGENTS.md` (generated from the plugin's schema). The vault is a cache, not a source of truth — every entry has a `volatility` field and a `verifications` log; check both before citing (see `meta/freshness-policy.md`).
