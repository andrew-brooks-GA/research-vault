# README images

Drop `obsidian-graph.png` here: a screenshot of a research vault opened in Obsidian
(graph view), showing entries as nodes and backlinks as edges. It is referenced from the
top-level `README.md` ("How it's organized"). Keep it reasonably small (a few hundred KB)
and refresh it if the view changes.